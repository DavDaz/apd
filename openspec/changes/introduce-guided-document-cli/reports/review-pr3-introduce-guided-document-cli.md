## Review

- **Verdict: ready-for-sdd-verify**

- **Correct:** Markdown export matches the PR 3 design/spec: it writes plain `.md` files under project-local `apd-docs/`, emits metadata (`Generated by`, `Template`, `Session`), preserves answered section order by iterating document sections, omits unanswered/skipped sections from the main body, and lists skipped/pending items in an `Open / Skipped Sections` appendix (`internal/generator/markdown.go:20-83`).
- **Correct:** AI Context Pack generation is non-fabricating: it only collects answered section content, maps it through template `context_keys`, and emits `Pending.` for headings without captured mapped content (`internal/generator/ai_context.go:39-64`). Tests cover captured-only context and pending absent headings (`internal/generator/markdown_test.go:64-81`).
- **Correct:** Final `apd new` export wiring preserves session state before Markdown export. Normal completion saves after each answer and then exports (`internal/app/new_document.go:97-115`); `/done` saves before export (`internal/app/new_document.go:104-123`); export failure returns the exporter error after prior session saves, with test coverage (`internal/app/new_document_test.go:98-107`).
- **Correct:** Verification config is sane for this Go CLI: `go test ./...` is configured for apply/verify and unit/typecheck, with `go vet ./...` configured as lint (`openspec/config.yaml:15-42`). I re-ran `go test ./...`, `go vet ./...`, and `git diff --check`; all passed.
- **Correct:** Scope boundary is respected. Code search found no AI/model calls, network/web/cloud/database integrations, TUI, custom template authoring, backlog generators, or prompt generators in Go code.
- **Correct:** Generated local output is ignored via `.gitignore` for `.apd/` and `apd-docs/` (`.gitignore:1-3`).

- **Minor:** `apd-docs/` being gitignored is safe for generated-output hygiene, but it is user-facing: users who want to commit generated Markdown will need to move/copy files or adjust `.gitignore`. This is already called out in the apply notes, so no PR 3 fix is required.

No blockers, majors, or nits found.
